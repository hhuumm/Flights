<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><% title %></title>
</head>
<body>
    <tbody>
        <tr>
                <td>AirLine</td>
                <td>Airport</td>
                <td>Flight #</td>
                <td>Departure</td>
                <td>DETAILS</td>
              </tr>
        <%if(flights!=null) {%>
        
              <% flights.forEach((f) => { %>
            <tr>
                <td><%= f.airline %></td>
                <td><%= f.airport %></td>
                <td><%= f.flightNo %></td>
                <td><%= f.departs %></td>
                <td><a href="/flights/<%= f.flightNo %>">DETAILS</a></td>
              </tr>
            <% }) %>
            <%} else{%>
                <tr><td>There are no flights in the directory</td></tr>
                <% } %>
      </tbody>
</body>
</html>